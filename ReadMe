**Shazam**

1. MusicFiles is the database against which Shazam performs song identification.
2. To test identification, open toplevel Matlab and type:

filename=searchDatabase(SongName);



************To add files to our existing Database**************

1. Add the mp3 file to MusicFiles directory.

2. Recompile make_database at top level with command:
[DataBase,FileDataBase]=make_database('MusicFiles');

3. Save the top level as 'MusicDataBase.mat'


